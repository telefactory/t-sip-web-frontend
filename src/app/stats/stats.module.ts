import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { CdrStatTableComponent } from './cdr-stat-table/cdr-stat-table.component';
// TODO: Declare and export here, but for now, we only need this component in one place

@NgModule({
  imports: [
  CommonModule,
  ],
  //declarations: [CdrStatTableComponent]
    declarations: []
})
export class StatsModule { }
